{
  "name": "geospark",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:server": "cd server && npm i && npm run build",
    "build:frontend": "cd frontend && npm i && npm run build",
    "build": "npm i && npm-run-all --parallel build:server build:frontend",
    "start:server": "cd server && npm start",
    "start:client": "cd frontend && npm start",
    "start": "npm-run-all --parallel start:server start:client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
